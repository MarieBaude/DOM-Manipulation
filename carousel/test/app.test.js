import chai from 'chai';
const assert = chai.assert;
import {changeSlide} from '../app.js';

describe('Slide mouvement', () => {
    it('should return ...', () => {
        assert.isOk(changeSlide(), 'azerty');
    })
}) 