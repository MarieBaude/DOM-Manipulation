import chai from 'chai';
const assert = chai.assert;
import {addingText} from '../app.js';

describe('Slide mouvement', () => {
    it('should return ...', () => {
        assert.isOk(addingText(), 'azerty');
    })
}) 